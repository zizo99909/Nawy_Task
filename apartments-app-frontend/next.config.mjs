/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['prod-images.cooingestate.com', 'saterdesign.com'], // TO BE CHANGED
  },
};

export default nextConfig;
